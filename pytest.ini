[pytest]
# Services are packages run with -m from the repo root, so the repo root is on
# the path here too and `import config`, `import catcher`, `import compactor`
# all resolve the same way they do at runtime.
pythonpath = .
testpaths = tests
addopts = --import-mode=importlib
